/**
 * Main test generator that orchestrates LLM calls and parsing
 */
import type { KakarotConfig } from '../types/config.js';
import type { TestGenerationContext, TestGenerationResult, TestFixContext } from '../types/llm.js';
export declare class TestGenerator {
    private provider;
    private fixProvider;
    private config;
    private modelContextLimit;
    private fixModelContextLimit;
    constructor(config: Pick<KakarotConfig, 'apiKey' | 'provider' | 'model' | 'fixModel' | 'maxTokens' | 'maxFixAttempts' | 'temperature' | 'fixTemperature' | 'customPrompts' | 'maxRetries' | 'contextLimit'>);
    /**
     * Generate test code for a test target
     */
    generateTest(context: TestGenerationContext): Promise<TestGenerationResult>;
    /**
     * Fix a failing test by generating a corrected version
     */
    fixTest(context: TestFixContext): Promise<TestGenerationResult>;
    /**
     * Generate test scaffold (minimal structure) for a test target
     */
    generateTestScaffold(target: TestGenerationContext['target'], existingTestFile?: string, framework?: 'jest' | 'vitest', testFilePath?: string, importPath?: string): Promise<TestGenerationResult>;
    /**
     * Generate a human-readable coverage summary
     */
    generateCoverageSummary(messages: Array<{
        role: 'system' | 'user' | 'assistant';
        content: string;
    }>): Promise<string>;
}
//# sourceMappingURL=test-generator.d.ts.map