/**
 * Merge new test code with existing test files intelligently
 */
export interface MergedTestFile {
    content: string;
    imports: Set<string>;
    describeBlocks: Map<string, string>;
}
/**
 * Parse an existing test file to extract structure
 */
export declare function parseTestFile(content: string): MergedTestFile;
/**
 * Extract imports from new test code
 */
export declare function extractImports(newCode: string): string[];
/**
 * Extract describe blocks from new test code
 */
export declare function extractDescribeBlocks(newCode: string): Map<string, string>;
/**
 * Extract non-import, non-describe code (setup, helpers, etc.)
 */
export declare function extractOtherCode(newCode: string): string;
/**
 * Merge new test code with existing test file
 */
export declare function mergeTestFiles(existingContent: string, newCode: string): Promise<string>;
/**
 * Check if a function/class already has tests in the file
 */
export declare function hasExistingTests(content: string, functionName: string, className?: string): boolean;
//# sourceMappingURL=test-file-merger.d.ts.map