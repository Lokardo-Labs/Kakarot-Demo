/**
 * Extract test targets from local git changes
 * Uses simple-git and reuses existing diff parsing logic
 */
import type { KakarotConfig } from '../types/config.js';
import type { TestTarget } from '../types/diff.js';
/**
 * Extract test targets from local git changes
 */
export declare function extractLocalTestTargets(config: Pick<KakarotConfig, 'testDirectory' | 'testFilePattern' | 'includePatterns' | 'excludePatterns'>): Promise<TestTarget[]>;
//# sourceMappingURL=local-file-analyzer.d.ts.map