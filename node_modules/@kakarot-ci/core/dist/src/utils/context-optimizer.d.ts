/**
 * Context optimizer for LLM requests
 * Truncates and optimizes content to fit within token limits
 */
/**
 * Extract key error message from full error output
 */
export declare function extractKeyErrorMessage(errorMessage: string, maxLength?: number): string;
/**
 * Extract relevant code sections from a file
 * Only includes the functions/classes that are actually being tested
 */
export declare function extractRelevantCode(fullCode: string, functionNames: string[], maxLength?: number): string;
/**
 * Extract only failing test cases from full test file
 */
export declare function extractFailingTests(fullTestCode: string, failingTestNames: string[], maxLength?: number): string;
/**
 * Optimize test fix context to fit within token limits
 */
export interface OptimizedFixContext {
    originalCode: string;
    testCode: string;
    errorMessage: string;
    testOutput?: string;
    failingTests?: Array<{
        testName: string;
        message: string;
        stack?: string;
    }>;
}
export declare function optimizeFixContext(context: {
    originalCode: string;
    testCode: string;
    errorMessage: string;
    testOutput?: string;
    failingTests?: Array<{
        testName: string;
        message: string;
        stack?: string;
    }>;
    functionNames?: string[];
}, maxTokens?: number): OptimizedFixContext;
//# sourceMappingURL=context-optimizer.d.ts.map