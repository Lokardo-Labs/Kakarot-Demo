/**
 * Validate generated test files for completeness and correctness
 */
export interface FileValidationResult {
    valid: boolean;
    errors: string[];
    warnings: string[];
    missingImports?: string[];
}
/**
 * Check if code is syntactically complete (balanced braces, complete statements)
 */
export declare function checkSyntaxCompleteness(code: string): {
    valid: boolean;
    errors: string[];
};
/**
 * Validate file using TypeScript compiler (if available)
 */
export declare function validateTypeScript(filePath: string, projectRoot: string): Promise<{
    valid: boolean;
    errors: string[];
    missingImports?: string[];
}>;
/**
 * Validate a generated test file for completeness and correctness
 */
export declare function validateTestFile(filePath: string, content: string, projectRoot: string, privateProperties?: string[]): Promise<FileValidationResult>;
//# sourceMappingURL=file-validator.d.ts.map